import { createSlice } from "@reduxjs/toolkit";


const newSlice = createSlice({
    initialState: {
        role: null
    },
    reducers:{
        findRole: (state)=>{
            
        }
    }

})